{* $Id$ *}
{if ! empty($maintenanceNotification)}
    <div class="bg-light-parent pt-4 pb-4 clearfix form-control">
        <div class="container container-std text-center">
            {$maintenanceNotification}
        </div>
    </div>
{/if}

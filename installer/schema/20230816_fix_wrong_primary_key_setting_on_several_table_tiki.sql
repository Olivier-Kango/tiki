/* The migration file has been replaced, but we have retained dummy query to prevent corruption on systems that have already executed the migration before the fix. */
SELECT * FROM tiki_schema LIMIT 1;